/** Hand-written types for a plain-JS module — see `tools/eslint/architecture.d.ts`. */

/** The exact text of `src/render/ui/title-key-art.ts`, for the drift test to compare against. */
export declare function renderModule(): string;
