import type { Plugin } from 'vite';

/**
 * Hand-written types for a plain-JS module — see `tools/room-editor/server.d.mts`
 * for why this stays JS rather than TS.
 */
export declare function artPipelineDevPlugin(): Plugin;
