{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "moduleDetection": "force",
    "types": ["vite/client"],

    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "exactOptionalPropertyTypes": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitReturns": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "useUnknownInCatchVariables": true,

    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "noEmit": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    "baseUrl": ".",
    "paths": {
      "@sim/*": ["src/sim/*"],
      "@render/*": ["src/render/*"],
      "@app/*": ["src/app/*"],
      "@content/*": ["src/content/*"],
      "@debug/*": ["src/debug/*"]
    }
  },
  "include": ["src", "tests", "tools", "vite.config.ts", "vitest.config.ts"],
  // Deliberate rule violations. They are linted by the architecture test, which
  // reads them as text and hands them to ESLint at a pretend path — they are
  // not meant to compile, and several of them import modules that do not exist
  // from where they sit.
  "exclude": ["tests/lint/fixtures"]
}
